# Cloudflare Pages 빌드에서 @cloudflare/next-on-pages 설치 시 wrangler 최신 버전이
# @cloudflare/workers-types 를 서로 다른 메이저(^4 vs ^5)로 요구해 ERESOLVE 로 실패한다.
# workers-types 는 빌드타임 타입 패키지라 런타임 영향이 없어 peer 충돌을 무시하고 설치한다.
legacy-peer-deps=true
